requests
fastapi
uvicorn[standard]
websockets
httpx
asyncssh
# pysnmp 7.x (NOT the abandoned pysnmp-lextudio fork, which is a deprecation
# stub whose pysnmp.hlapi holds only asyncio/). src/transports/snmp_io.py was
# ported to the v1arch asyncio API in nw#103; 7.x accepts pyasn1 0.6.4, which
# retires the pyasn1/pyasn1-modules pins that used to be required here. Those
# pins existed only because lextudio 6.3.0 wanted pyasn1<0.6.1 while
# pyasn1-modules>=0.4.2 wanted >=0.6.1 — mutually exclusive, and the agent's
# per-role pip runs thrashed between them.
pysnmp>=7.1.0
